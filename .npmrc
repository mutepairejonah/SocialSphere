# NPM Configuration for VSCode Development

# Use exact versions to avoid compatibility issues
save-exact=false

# Allow optional dependencies (better-sqlite3, etc)
optional=true

# Progress bar for visibility
progress=true

# Production environment
# production=false

# Registry (use default npm registry)
registry=https://registry.npmjs.org/
